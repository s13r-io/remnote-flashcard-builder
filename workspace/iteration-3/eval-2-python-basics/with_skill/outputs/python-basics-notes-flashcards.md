Python :: A high-level, interpreted programming language known for its simple and readable syntax
creator ;; Guido van Rossum
first released ;; 1991
Python emphasizes {{code readability}} and allows programmers to express concepts in fewer lines of code.
Which of the following best describes Python? >>A) High-level, interpreted language B) Low-level, compiled language C) Assembly language D) Markup language
Variable :: A named location used to store data in memory
Python is {{dynamically}} typed, meaning you don't need to declare a variable's type before using it.
Main data types in Python >>>
Integers (whole numbers)
Floats (decimal numbers)
Strings (text)
Booleans (True or False)
Lists (ordered collections)
Dictionaries (key-value pairs)
Tuples (immutable sequences)
Which data type is an ordered, mutable collection in Python? >>A) List B) Tuple C) String D) Dictionary
Which data type is an ordered, immutable sequence in Python? >>A) Tuple B) List C) Dictionary D) Set
Which data type stores key-value pairs in Python? >>A) Dictionary B) List C) Tuple D) String
String :: A sequence of characters enclosed in quotes
List :: A mutable sequence of items enclosed in square brackets
Tuple :: An immutable sequence of items enclosed in parentheses
Strings are enclosed in {{quotes}}, lists in {{square brackets}}, and tuples in {{parentheses}}.
Which Python type is mutable: string, list, or tuple? >>A) List B) String C) Tuple D) All three
Which Python type is immutable and uses parentheses? >>A) Tuple B) List C) Dictionary D) Set
Both lists and tuples can contain {{any data type}}.
When should you use a list over a tuple? >> When you need to modify the data after creation
When should you use a tuple over a list? >> When you need a collection that cannot be changed
Ordered, mutable sequence enclosed in square brackets << List
Ordered, immutable sequence enclosed in parentheses << Tuple
Mutable <> Can be changed after creation
Immutable <> Cannot be changed after creation
Dictionary :: An unordered collection of key-value pairs enclosed in curly braces
Each key in a dictionary must be {{unique}}.
Dictionaries are accessed by {{key}}, while lists are accessed by {{index}}.
Are dictionaries mutable or immutable? >> Mutable — you can add, remove, or modify key-value pairs after creation
Which of the following is true about dictionaries? >>A) They are accessed by key B) They are accessed by index C) Keys can be duplicated D) They are immutable
Function :: A reusable block of code that performs a specific task
Functions in Python are defined using the {{def}} keyword.
What follows the def keyword when defining a function? >> A name, parentheses, and a colon
Parameters <> Inputs to a function
Return values <> Outputs from a function
The {{return}} statement sends a value back from a function to the code that called it.
Which keyword is used to define a function in Python? >>A) def B) func C) function D) define
Loop :: A construct that allows you to repeat a block of code multiple times
Two types of loops in Python >>>
for loops (iterate over a sequence a known number of times)
while loops (repeat as long as a condition is true)
for loop <> Iterates over a sequence a known number of times
while loop <> Repeats as long as a condition is true
Which loop type iterates over a sequence like a list or string? >>A) for loop B) while loop C) do-while loop D) repeat loop
The {{break}} statement stops a loop immediately.
The {{continue}} statement skips the current iteration and moves to the next one.
Which statement stops a loop immediately? >>A) break B) continue C) pass D) return
Which statement skips the current iteration of a loop? >>A) continue B) break C) pass D) exit
Conditional statement :: A statement that runs different code depending on whether a condition is true or false
Python conditional keywords >>>
if
elif (else if)
else
An {{if}} statement executes code only if the condition is true.
An {{elif}} statement provides an alternative condition to check if the previous if was false.
An {{else}} statement executes code if all previous conditions were false.
Which keyword provides an alternative condition after an if statement? >>A) elif B) else C) then D) otherwise
Common list methods in Python >>>
append() — add an item
remove() — delete an item
pop() — remove an item by index
insert() — add an item at a specific position
sort() — arrange items in order
len() — count the number of items
append() ;; Adds an item to a list
remove() ;; Deletes an item from a list
pop() ;; Removes an item from a list by index
insert() ;; Adds an item at a specific position in a list
sort() ;; Arranges list items in order
len() ;; Counts the number of items in a list
Which method adds an item at a specific position in a list? >>A) insert() B) append() C) add() D) push()
Which method removes an item by index from a list? >>A) pop() B) remove() C) delete() D) discard()
Object-Oriented Programming (OOP) :: A programming paradigm based on the concept of objects, which can contain data and code
Class :: A blueprint for creating objects
Instance :: A specific object created from a class
Method ;; A function defined inside a class that operates on the data within an object
Inheritance :: The mechanism that allows a class to inherit properties and methods from another class, promoting code reuse
A blueprint for creating objects << Class
A specific object created from a class << Instance
Which OOP concept allows a class to reuse properties and methods from another class? >>A) Inheritance B) Encapsulation C) Polymorphism D) Abstraction
Exception :: An error that occurs during program execution
Common Python exceptions >>>
NameError (variable not defined)
TypeError (wrong data type)
ValueError (wrong value for operation)
IndexError (trying to access an index that doesn't exist)
NameError ;; Raised when a variable is not defined
TypeError ;; Raised when the wrong data type is used
ValueError ;; Raised when the wrong value is used for an operation
IndexError ;; Raised when trying to access an index that doesn't exist
The {{try-except}} block allows you to handle exceptions gracefully instead of letting the program crash.
Which exception occurs when you access an index that doesn't exist? >>A) IndexError B) ValueError C) TypeError D) NameError
Which exception occurs when a variable is not defined? >>A) NameError B) ValueError C) TypeError D) IndexError
Which exception occurs when the wrong data type is used? >>A) TypeError B) ValueError C) NameError D) IndexError
Trying to access a non-existent index in a list << IndexError
Using a variable that has not been defined << NameError
