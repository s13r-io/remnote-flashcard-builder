Python :: A high-level, interpreted programming language known for its simple and readable syntax
creator of Python ;; Guido van Rossum
year Python was first released ;; 1991
Python emphasizes >> Code readability and expressing concepts in fewer lines of code than C++ or Java
What are the main data types in Python? >>> Integers, Floats, Strings, Booleans, Lists, Dictionaries, Tuples
Variable :: A named location used to store data in memory
Python is {{dynamically typed}}, meaning you don't need to declare a variable's type before using it.
Integer :: A whole number data type in Python
Float :: A decimal number data type in Python
String :: A sequence of characters enclosed in quotes
Boolean :: A data type with two possible values: True or False
List >> Is mutable or immutable? A) Mutable B) Immutable
Tuple >> Is mutable or immutable? A) Immutable B) Mutable
String vs List: Which is mutable? << List
Tuple vs List: Which requires square brackets for creation? << List
String <> Sequence of characters enclosed in quotes
List <> Ordered, mutable sequence of items enclosed in square brackets
Tuple <> Ordered, immutable sequence of items enclosed in parentheses
Dictionary :: An unordered collection of key-value pairs enclosed in curly braces
key in a dictionary ;; Must be unique and is used to retrieve its associated value
dictionaries are {{mutable}}, so you can add, remove, or modify key-value pairs after creation.
Lists are accessed by {{index}}, while dictionaries are accessed by {{key}}.
Lists are preferred when >> You need to modify data
Tuples are useful when >> You need a collection that cannot be changed
Function :: A reusable block of code that performs a specific task
Functions are defined using >> The def keyword followed by a name, parentheses, and a colon
What does a parameter do in a function? >> It serves as an input to the function
What does a return statement do? >> It sends a value back from the function to the code that called it
Loop :: A control structure that allows you to repeat a block of code multiple times
For loop >> Iterates over a sequence a known number of times
While loop >> Repeats as long as a condition is true
break statement ;; Stops a loop immediately
continue statement ;; Skips the current iteration and moves to the next one
Conditional statements allow you to >> Run different code depending on whether a condition is true or false
if statement ;; Executes code only if the condition is true
elif statement ;; Provides an alternative condition to check if the previous if was false
else statement ;; Executes code if all previous conditions were false
Python has {{two}} main types of loops: {{for}} loops and {{while}} loops.
List methods >> What are the common methods for modifying or inspecting lists? >>> append() to add an item, remove() to delete an item, pop() to remove by index, insert() to add at a position, sort() to arrange in order, len() to count items
append() ;; Adds an item to a list
remove() ;; Deletes an item from a list
pop() ;; Removes an item from a list by index
insert() ;; Adds an item at a specific position in a list
sort() ;; Arranges items in a list in order
len() <> Function that counts the number of items in a list or string
Object-Oriented Programming (OOP) :: A programming paradigm based on the concept of objects that can contain data and code
Class :: A blueprint for creating objects in OOP
Instance :: A specific object created from a class
Method ;; A function defined inside a class that operates on the data within an object
Inheritance :: A mechanism allowing a class to inherit properties and methods from another class, promoting code reuse
What are the main components of OOP? >>> Objects, Classes, Methods, Inheritance, Encapsulation
Exception :: An error that occurs during program execution
NameError :: An exception raised when a variable is not defined
TypeError :: An exception raised when an operation is applied to a value of the wrong data type
ValueError :: An exception raised when a function receives an argument of the correct type but an inappropriate value
IndexError :: An exception raised when trying to access an index that doesn't exist in a sequence
try-except block >> A structure that allows you to handle exceptions gracefully instead of letting the program crash
Strings and tuples are both {{immutable}}, meaning their contents cannot be changed after creation.
Which data type uses curly braces for creation? A) List B) Dictionary C) Tuple D) String >> B) Dictionary
What is the main difference between a for loop and a while loop? >> A for loop iterates a known number of times over a sequence, while a while loop repeats as long as a condition is true
Can you explain the relationship between classes and objects? >> A class is a blueprint or template, while an object is a specific instance created from that class
Which of the following is immutable? A) List B) Dictionary C) Tuple D) Set >> C) Tuple
In Python, what does the {{return}} statement do in a function? >> It sends a value back from the function
